<UserControl xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="AvaloniaXplat.Views.InstallingWindow"
		x:DataType="viewModels:InstallingWindowViewModel"
        xmlns:viewModels="clr-namespace:AvaloniaXplat.ViewModels">
	<StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
		<Border Background="#FFF" CornerRadius="4" Padding="10" Margin="5"
                BorderBrush="#FF000B25" BorderThickness="1" >
			<StackPanel>
				<TextBlock x:Name="StatusTextBlock"
                           Text="{Binding StatusText}"
                           FontWeight="Bold"
                           Margin="0,10"
                           HorizontalAlignment="Center"/>
				<ProgressBar IsIndeterminate="True" Height="20" Width="250"/>
			</StackPanel>
		</Border>
	</StackPanel>
</UserControl>

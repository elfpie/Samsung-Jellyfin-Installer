<UserControl xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:AvaloniaXplat.ViewModels"
        mc:Ignorable="d"
        x:Class="AvaloniaXplat.Views.InstallationCompleteWindow"
        x:DataType="viewModels:InstallationCompleteViewModel"
        Background="#F5F5F5">
	<!-- Solid background -->

 	<UserControl.Styles>
 		<StyleInclude Source="avares://Avalonia.Themes.Fluent/FluentTheme.xaml"/>
 		<StyleInclude Source="avares://Avalonia.Controls.DataGrid/Themes/Fluent.xaml"/>
 		<StyleInclude Source="avares://AvaloniaXplat/Styles/Buttons.axaml"/>
 		<StyleInclude Source="avares://AvaloniaXplat/Styles/TextBlocks.axaml"/>
 		<StyleInclude Source="avares://AvaloniaXplat/Styles/ComboBoxes.axaml"/>
 	</UserControl.Styles>

	<Border Background="White"
            Padding="10"
            CornerRadius="8"
            Margin="20"
            BorderBrush="#E8E8E8"
            BorderThickness="1">
		<Border.Effect>
			<DropShadowEffect BlurRadius="15"
                              OffsetX="2" OffsetY="2"
                              Color="#00000020"
                              Opacity="0.15"/>
		</Border.Effect>

		<StackPanel Margin="20" HorizontalAlignment="Center" Spacing="14">

			<!-- ✅ Icon -->
			<TextBlock Text="✅"
                       FontSize="40"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"/>

			<!-- Success Message -->
			<TextBlock Text="{Binding SuccessMessage}"
                       TextWrapping="Wrap"
                       FontSize="16"
                       TextAlignment="Center"
                       Foreground="#2C3E50"
                       FontWeight="Medium"/>

			<!-- Extra Info -->
			<TextBlock Text="{Binding EasyRight}"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Foreground="#7F8C8D"
                       FontSize="14"/>

			<!-- Action Buttons -->
			<StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Spacing="20"
                        Margin="0,12,0,0">

				<!-- Ko-fi Button -->
				<Button Content="{Binding Validation}"
                        Command="{Binding OpenKoFiCommand}"
                        Background="#2C3E50"
                        Foreground="White"
                        BorderThickness="2"
                        CornerRadius="6"
                        Padding="15,8"
                        FontWeight="Medium"
                        Cursor="Hand"/>

				<!-- Close Button -->
				<Button Content="{Binding Close}"
                        Command="{Binding CloseWindowCommand}"
                        Background="#2C3E50"
                        Foreground="White"
                        BorderThickness="0"
                        CornerRadius="6"
                        Padding="15,8"
                        Width="90"
                        FontWeight="Medium"
                        Cursor="Hand"/>
			</StackPanel>
		</StackPanel>
	</Border>
</UserControl>
